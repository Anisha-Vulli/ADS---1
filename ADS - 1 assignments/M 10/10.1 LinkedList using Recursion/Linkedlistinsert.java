// if (pos == 0) {
        //     //newNode.data = data;
        //     newNode.next = head;
        //     head = newNode;
        //     size++;
        //     return;
        // }
        // 
        // if (size++ == 0) {
        //     head.data = data;
        //     return;
        // }

        // Node previous = null;
        // Node temp = head;
        // int count = 0;
        // while (temp != null) {
        //     if (count == pos) {
        //         previous.next = newNode;
        //         newNode.next = temp;
        //         size++;
        //         return;
        //     }
        //     previous = temp;
        //     temp = temp.next;
        //     count++;
        // }
        